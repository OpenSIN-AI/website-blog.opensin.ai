{
  "name": "website-blog.opensin.ai",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "bun install && vite build",
    "build:vitepress": "bunx vitepress build post",
    "preview": "vite preview",
    "preview:vitepress": "vitepress preview post",
    "clean": "rm -rf .vitepress/dist dist",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "typescript": "~5.8.2"
  },
  "license": "Apache-2.0"
}
